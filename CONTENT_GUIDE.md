# CONTENT_GUIDE.md — как добавить SEO-статью на Georgia Guidebook

Рабочая инструкция для регулярного написания статей. **Источники правды по продукту/правилам:** `SPEC.md` → `CLAUDE.md` → этот файл. Если что-то противоречит — приоритет у SPEC/CLAUDE.

> TL;DR: статья = Markdown-файл в `src/content/articles/<lang>/<slug>.md` (Astro Content Collections). RU-версия лежит в `…/ru/…` и автоматически отдаётся на `/ru/<категория>/<slug>/`. **Публикуется только парой ru + uk** (en — по желанию), иначе не пройдёт гейт. Перед публикацией — `npm run qa` → должно быть **GO**.

---

## 0. Для владельца: как это работает (простыми словами)

**Главное: статьи пишешь НЕ ты.** Их пишу я: черновик ru → перевод uk/en → фото → **взаимная перелинковка** (новая статья ссылается на соседей+хаб — это гейт; И опорная/соседи кластера ссылаются на неё, во всех 3 языках — мой ручной шаг, гейтом не ловится) → проверка фактов → `npm run qa` (= GO) → коммит + публикация. Твоя роль короткая:

1. **Дать факты, которых нет в открытых источниках** — особенно местное по Батуми/Аджарии (любимые места, цены по чеку, что реально стоит/не стоит, что закрылось). Без этого я пишу только на **проверяемых фактах** из надёжных источников — без выдуманного «я там был».
2. **Дать свои фото** (по желанию) — они дают метку «Проверено» и сильнее в Google, чем сток. Нет своих → беру бесплатные CC.
3. **Сказать «ок»** на публикацию.

**Как заказать статью:** просто напиши «пиши про &lt;тему&gt;» — я беру тему из плана, собираю факты, пишу тройку ru/uk/en, прогоняю гейты и публикую. Или кинь мне факт/место — я превращу в контент.

**По какому плану:** очередь и приоритеты — `docs/CONTENT_PLAN.md` (§0 — стратегия, дальше кластеры/очередь). Порядок исполнения — `ROADMAP-GROWTH-2026.md`. Куда движемся — память `growth-strategy-direction`.

**Что пишем сейчас (ближайшее):**
1. Углубить гайды еды (Тбилиси/Батуми/Кутаиси) — уже есть, делаю содержательнее.
2. Гайд «вино Кахетии» (бриф готов) — мост в винные туры (деньги).
3. Цена/часы (`visit`) + фактические заметки у топ-достопримечательностей и страниц Батуми — против AI Overviews.
4. Английский пилот для экспатов (1% налог, work permit 2026, стоимость жизни) — бонус-рынок.

**Правило фактов (железное, §15):** цены/адреса/часы — только из надёжного источника + «уточняйте» + дата; нет данных → не выдумываем. Это защита от бана Google.

---

## 1. Как технически устроены статьи

- **Движок:** Astro 6 + **Content Collections**. Статьи — это **Markdown** (`.md`/`.mdx`) с YAML-frontmatter. Отдельные `.astro`-файлы под каждую статью НЕ создаются — рендерит общий шаблон `src/layouts/ArticlePage.astro` через динамический маршрут.
- **Где лежат файлы:**
  ```
  src/content/articles/ru/<slug>.md   ← русская версия
  src/content/articles/uk/<slug>.md   ← украинская (обязательна, см. §3)
  src/content/articles/en/<slug>.md   ← английская (по желанию)
  ```
  (Маршруты — отдельная коллекция `src/content/routes/`; «Где поесть» — `src/content/restaurants/`; услуги — `src/content/services/`; города-карточки — `src/content/cities/`. Эта инструкция — про **статьи** в `articles/`.)
- **Как файл попадает в нужный раздел:** разделом управляет поле **`category`** во frontmatter, а НЕ папка. URL строится как `/<lang-префикс>/<category>/<slug>/`. Файл `articles/ru/abc.md` с `category: 'goroda'` → страница `/ru/goroda/abc/` и автоматически попадает в хаб `/ru/goroda/` (хаб сам подтягивает статьи своей категории — ручных списков нет).
- **Допустимые `category`** (enum в `src/content.config.ts`):
  `dostoprimechatelnosti` · `goroda` · `eda` · `razvlecheniya` · `marshruty` · `transport` · `arenda-avto` · `relokatsiya` · `strahovka` · `novosti`.
  - Для обычных SEO-статей чаще всего: **goroda, dostoprimechatelnosti, transport, arenda-avto, relokatsiya, novosti, razvlecheniya**.
  - Нюансы: `marshruty` — это отдельная коллекция `routes/` (не `articles/`); `eda` — директория мест (`restaurants/`), а не лонгрид; `strahovka` — раздел с кастомным хабом, но статьи категории `strahovka` в `articles/` тоже работают (`/ru/strahovka/<slug>/`).

---

## 2. Frontmatter статьи (поля)

Полная схема — `src/content.config.ts` (`articleBase`). Главное:

| Поле | Обяз.? | Что это |
|---|---|---|
| `title` | да | Заголовок = **H1** на странице. **≤ 60 символов** (гейт `check-parity`). Должен содержать целевой запрос (см. §8). |
| `description` | да | Meta description. **≤ 155 символов** (валидируется схемой). 1–2 предложения с запросом. |
| `slug` | да | Латиница-цифры-дефис. **Стабилен навсегда** (URL не меняем — правило 3). Одинаковый во всех языках. |
| `lang` | да | `'ru'` / `'uk'` / `'en'` — должен совпадать с папкой. |
| `category` | да | Один из enum выше → определяет раздел и URL. |
| `publishedAt` | да | Дата публикации `YYYY-MM-DD`. |
| `updatedAt` | да | Дата обновления `YYYY-MM-DD` (видна на странице — «Обновлено · …»). Поднимай при правках фактов. |
| `cover` | да* | Обложка: `src` (путь к webp) + `alt`. *Фото обязательны (правило проекта); гейт `check-photos` валит сборку без существующего cover ≤200 КБ. |
| `coverCredit` | для CC | Атрибуция обложки «Фото: Автор / источник, лицензия» — обязательна для CC/чужих фото (§18). |
| `gallery` | нет | Массив `{src, alt, credit?}` — мини-галерея с лайтбоксом. |
| `affiliate` | нет | 1–3 партнёрских бокса (см. §7). По умолчанию `[]`. |
| `sources` | нет | Массив доменов/ссылок-первоисточников фактов (§14). |
| `verifiedAt` | нет | Дата реальной сверки места → бейдж «Проверено · …». Без сверки — НЕ ставить (будет «Обновлено», правило 4). |
| `geo` | нет | `{ coord: [lat, lng], address? }` — для статей о местах (даёт карту + schema). |
| `accessFrom` | нет | «Как доехать» из Тбилиси/Кутаиси/Батуми (`{km?, duration?, note?}`). |
| `visit` | нет | «Вход и часы»: `{price?, hours?, note?, checkedAt?}`. Цены/часы НЕ выдумывать — только источник + дата + «уточняйте». |
| `featuredOrder` | нет | Меньше число = выше в хабе раздела. |
| `attractionType` / `region` | нет | Только для `dostoprimechatelnosti` (фильтры каталога). Слаги — в `content.config.ts`. |
| `razvlType` | нет | Только для `razvlecheniya` (`kazino`/`mesta`/`nochnaya-zhizn`/`afisha`/`aktivnyy`). |
| `tags` | нет | Свободные теги (показ в смежных разделах). |
| `hotelWidget` | нет | `true` → виджет отелей Trip.com (для городов с высоким спросом на жильё). |
| `draft` | да на старте | `true` = скелет, в сборку НЕ попадает. Снять (`false`) только когда статья готова и гейты зелёные. |

**Примеры (реальные статьи — посмотри их целиком):**
- `src/content/articles/ru/kak-vybrat-strahovku.md` — справочная статья: cover + `affiliate` (SafetyWing) + `sources` + внутренние ссылки.
- `src/content/articles/ru/mestia.md` — статья о месте: `geo`, `gallery`, инлайн-фигуры, `accessFrom`.
- `src/content/articles/ru/tbilisi.md` — город: `featuredOrder`, `hotelWidget`, `visit`.

---

## 3. Мультиязычность (EN/RU/UK) и hreflang

- **EN — язык по умолчанию, на корне `/`.** RU → `/ru/…`, UK → `/uk/…`. (Префикс задаёт `langPrefix()` в `src/i18n/index.ts`: `DEFAULT_LANG='en'`. NB: пара комментариев в коде ещё говорит «ru — корень» — это устаревшая надпись, рантайм отдаёт en на корне.)
- **Чтобы RU-статья попала в `/ru/`:** файл в `src/content/articles/ru/<slug>.md`, `lang: 'ru'`, `category: '<…>'`. URL получится `/ru/<category>/<slug>/` автоматически (маршрут `src/pages/ru/[category]/[slug].astro`).
- **🔴 Главное правило (CLAUDE правило 1):** статья публикуется **парой ru + uk** с **одинаковым `slug`** (en — по желанию/батчами). RU-only **не пройдёт** гейт `check-parity` (он требует и ru, и uk). Половину пары не публиковать.
- **hreflang — автоматический.** `BaseLayout` сам проставляет взаимные `<link rel="alternate" hreflang="…">` + `x-default` по тем языкам, где **существует тот же `slug`**. От тебя нужно только: одинаковый slug в ru/uk(/en) — и связка ru↔uk построится сама. Переключатель языка тоже работает по совпадающему slug (`mirrorPath`).
- **Перевод UK:** адаптируй под украинскую аудиторию (а не дословно): EKTA вместо недоступных страховок, сухопутный въезд через Сарпи, валютные нюансы. Числовые поля (даты, цифры) и `slug`, `cover.src`, `gallery` src — одинаковые в паре.

---

## Как делать UK-версию правильно

UK-версия — обязательная пара к RU (правило 1) и **полноценная статья на украинском**, а не технический довесок:

1. **Живой украинский — не калька и не суржик.** Текст должен читаться как написанный носителем украинского: естественные конструкции, украинская лексика и идиомы. НЕ пословный перевод русского и НЕ смесь языков — переводим смысл и тон, а не слова.
2. **Факты идентичны RU.** Цены, цифры, расстояния, даты, названия — те же, что в RU-версии (как и `slug`, `cover.src`, `gallery` src, числовые поля frontmatter). Расхождение фактов между версиями недопустимо.
3. **H1 = украинский целевой запрос.** `title` украинской версии содержит то, **как ищут носители украинского в Google** (украинский поисковый запрос), а НЕ перевод русского H1. При этом `title` **≤ 60 символов**: если запрос длинный — бери его **ядро в H1**, а длинный хвост раскрывай в подзаголовках H2/H3. Аналогично — украинская `description` (≤155) под украинский запрос.
4. **Внутренние ссылки — на UK-страницы.** В UK-версии все внутренние ссылки с префиксом `/uk/…` (а не `/ru/…`). Минимум 2, как и в RU.
5. **Тело — украинские H2/H3** под смежные украинские запросы, своя лид-фраза, подписи к фото на украинском. Кредиты фото после «Фото:» (автор/лицензия) НЕ переводим — оставляем дословно.

**EN-версия — опциональна** (сайт её не требует; делаем выборочно для высокочастотных/международных тем). Если делаем — те же правила: **адаптация под английский запрос**, не дословный перевод; `title` ≤60 под английский запрос; внутренние ссылки на корне (`/…`, без префикса); факты идентичны RU/UK.

---

## 4. Пошагово: добавить RU-статью

1. **Бриф/факты.** Собери факты заранее (цены, адреса, часы — из надёжных источников). Нет факта → ставь `<!-- TODO -->` и не выдумывай (правило 4). Можно завести `briefs/<slug>.md`.
2. **Скелет (рекомендуется).** Из корня проекта:
   ```
   npm run new -- article <slug> --category <category> --title "Заголовок ≤60 символов"
   ```
   Создаст `articles/{ru,uk,en}/<slug>.md` (схема-корректный frontmatter, `draft: true`, DoD-чеклист в комментарии) + папку фото `public/images/<slug>/`. Существующие файлы не перезатирает.
   *(Альтернатива — скопировать существующую статью и поменять frontmatter.)*
3. **Текст RU.** Заполни `articles/ru/<slug>.md`:
   - `description` (≤155), `publishedAt`/`updatedAt`.
   - **Тело:** начинается с лид-абзаца (это первый абзац, без `#`), затем секции `## H2` / `### H3`. **`#`/H1 в теле НЕ ставить** — H1 рендерится из `title`.
   - Соблюдай SEO-требования из §8.
4. **Фото** (§6) — cover обязателен + 2–4 инлайн-фигуры или галерея.
5. **Внутренние ссылки** (§7) — минимум 2 на смежные статьи + ссылка на хаб раздела.
6. **Перевод UK** — создай/заполни `articles/uk/<slug>.md` (тот же slug). EN — по желанию.
7. **Сними `draft`** → `draft: false` в ru и uk.
8. **Гейты** (§9) — `npm run qa` → **GO**.
9. **Коммит пары** ru+uk одним коммитом → push в `main` (Cloudflare соберёт и задеплоит).

---

## 5. Куда положить файл — кратко

| Тип | Папка | URL (RU) | Раздел |
|---|---|---|---|
| Статья | `src/content/articles/ru/<slug>.md` | `/ru/<category>/<slug>/` | по полю `category` |
| Новость | `articles/ru/<slug>.md`, `category: 'novosti'` | `/ru/novosti/<slug>/` | Новости |

---

## 6. Фотографии

- **Уникальные на статью (правило владельца 2026-06-19):** у каждой статьи СВОИ фото — **НЕ переиспользовать кадры из других статей** (`/images/<другой-slug>...`). Под новую статью искать новые кадры через пайплайн и класть в свою папку. Проверять, что НА фото (не выдавать чужую страну/валюту за грузинскую). Память `article-photos-unique`.
- **Где лежат:** `public/images/<slug>/` (создаётся скаффолдером). Формат — **`.webp`**, каждое **≤ 200 КБ** (перф-бюджет §15), с заданными `width`/`height` (нулевой CLS).
- **Откуда брать:** только свои фото или **CC/PD** (Wikimedia / Openverse / Unsplash). Скрипты-помощники: `scripts/commons-candidates.mjs`, `scripts/openverse-candidates.mjs`, `scripts/unsplash-candidates.mjs` (поиск кандидатов) → `scripts/build-gallery.mjs` / `build-unsplash.mjs` (скачать + сжать в webp ≤200 КБ). Атрибуцию (автор+лицензия) бери **дословно из источника**, не из имени файла.
- **Обложка (frontmatter):**
  ```yaml
  cover:
    src: '/images/<slug>/cover.webp'
    alt: 'Осмысленное описание кадра'
  coverCredit: 'Фото: Имя Автора / Wikimedia Commons, CC BY-SA 4.0'
  ```
- **Инлайн-фото в тексте** (ключевые кадры — прямо в теле, с подписью). Точная разметка (raw HTML в .md):
  ```html
  <figure class="figure">
  <img src="/images/<slug>/g1.webp" alt="Что на фото" width="1280" height="853" loading="lazy" decoding="async" />
  <figcaption>Подпись — что на фото. <span class="figure__credit">Фото: Автор / источник, лицензия</span></figcaption>
  </figure>
  ```
  `width`/`height` = реальные пиксели файла (иначе перекос/CLS).
- **Мини-галерея внизу** (необязательные кадры) — через frontmatter:
  ```yaml
  gallery:
    - src: '/images/<slug>/g2.webp'
      alt: '…'
      credit: 'Фото: …'
  ```
  Каждое фото используется **один раз** (либо инлайн, либо в галерее).
- **`alt`** — осмысленный, по делу (не «фото1»); описывает содержимое кадра.
- **После добавления обложек** прогони `npm run build:covers` — сгенерит адаптивные варианты (`srcset`) и обновит манифест `src/data/cover-variants.json` (мобайл грузит меньший размер). Варианты коммитятся.

---

## 7. Внутренние ссылки

- **Формат (RU):** обычная Markdown-ссылка с **префиксом языка** и завершающим слэшем:
  ```markdown
  Подробнее — в путеводителе по [Тбилиси](/ru/goroda/tbilisi/) и в гайде по [аренде авто](/ru/arenda-avto/).
  ```
  Для UK-версии — `/uk/…`, для EN — без префикса (`/goroda/tbilisi/`).
- **Минимум 2** внутренние ссылки в теле на смежные статьи — это **гейт** `check-interlinks` (DoD §8.1). Партнёрские `/go/`-ссылки и якоря в счёт не идут.
- Ставь ссылку и **на хаб раздела** (`/ru/<category>/`), и желательно встречную ссылку из смежной статьи (двунаправленный кластер).
- **Партнёрские ссылки — ТОЛЬКО через `/go/`** (правило 2), и только во frontmatter `affiliate` (рендерится как AffiliateBox), не прямыми URL в тексте:
  ```yaml
  affiliate:
    - partner: 'trip-hotels'      # ключ из src/data/partners.json
      label: 'Где остановиться'
      position: 'body-end'
  ```

---

## 8. SEO-требования к статье (соблюдаем ВСЕГДА)

1. **H1 = целевой запрос.** Поле `title` (= H1) точно содержит целевой поисковый запрос. Ограничение: **≤ 60 символов** (гейт). Один H1 на странице (он из `title`; в теле `#` не использовать).
2. **Первый абзац — прямой ответ на запрос в 2–3 предложениях.** Лид сразу даёт суть, без «разгона».
3. **Подзаголовки `## H2` / `### H3` со смежными запросами** — структура под вопросы читателя (что/где/как/сколько/когда). Логичная иерархия без скачков уровней.
4. **Таблицы** для цен / расстояний / сравнений — обычные Markdown-таблицы (`| … | … |`). Числа — только из источника (правило 4), цены — с «уточняйте» + дата.
5. **2–4 фото** с осмысленными `alt` и подписями (figcaption) — см. §6.
6. **Блок внутренних ссылок** на смежные статьи — минимум 2 (см. §7), плюс хаб раздела.
7. **Дата обновления** — поле `updatedAt` (видно на странице). Поднимать при правке фактов.
8. **Объём 1200–2000 слов, без «воды».** Каждый абзац несёт пользу; никакой генерёнки и near-duplicate (§15 — риск бана).
9. **Доп. требования проекта:** `description` ≤155 с запросом; факты не выдумывать (правило 4) — нет данных → TODO/«уточняйте» + источник в `sources`; уникальная фактура (≥3 своих факта), не перепечатка; **FAQ не используем** (убран со всего сайта — новую инфо вплетать в тело).

---

## 9. Проверка перед публикацией (гейты)

Из корня проекта:

| Команда | Что проверяет |
|---|---|
| `npm run check` | типы + соответствие frontmatter схеме |
| `npm test` | enums · **parity (есть ли пара ru+uk, slug, title ≤60)** · photos (cover ≤200 КБ) · **interlinks (≥2)** |
| `npm run test:links` | внутренние ссылки резолвятся + `/go/`-партнёры валидны |
| `npm run lint` | стиль/ESLint |
| **`npm run qa`** | **всё вышеперечисленное + чистая сборка + аудит `dist/` → вердикт GO/NO-GO** |

**Правило:** перед коммитом — `npm run qa` = **GO**. Затем коммит пары ru+uk + push в `main`.

**Как убедиться, что статья в `/ru/` с корректным hreflang:**
1. `npm run build` → проверь, что появился `dist/ru/<category>/<slug>/index.html`.
2. В нём есть `<link rel="alternate" hreflang="ru" …>` и `hreflang="uk" …>` (+ `en`/`x-default`, если есть перевод) — проставляется автоматически по совпадающему slug.
3. `npm run test:links` — без битых ссылок.

---

## 10. Чек-лист публикации (быстрый)

- [ ] `articles/ru/<slug>.md` и `articles/uk/<slug>.md` — одинаковый `slug`, `category`, числа.
- [ ] `title` ≤60 и содержит целевой запрос; `description` ≤155.
- [ ] Лид отвечает на запрос в 2–3 предложениях; H2/H3 по смежным запросам; объём 1200–2000 слов.
- [ ] Таблица(ы) где уместно; факты с источниками (`sources`), без выдумки.
- [ ] Обложка + 2–4 фото (webp ≤200 КБ, alt + подписи + кредиты); `npm run build:covers` прогнан.
- [ ] ≥2 внутренние ссылки + ссылка на хаб; партнёрские — только через `/go/`.
- [ ] `updatedAt` актуальна; `draft: false` в обеих версиях.
- [ ] `npm run qa` → **GO**; коммит пары одним коммитом + push в `main`.

---

## 11. Болванка-шаблон статьи (копируй как старт)

Скопируй в `src/content/articles/ru/<slug>.md` и заполни. Для UK — то же в `…/uk/<slug>.md` по правилам раздела «Как делать UK-версию правильно». H1 — это поле `title` (в теле `#` НЕ ставим). Лишние опциональные поля (geo/visit/accessFrom) добавляй по необходимости из §2.

```markdown
---
title: 'Целевой запрос в заголовке ≤60 символов'
description: 'Ответ на запрос в 1–2 предложениях, ≤155 символов.'
slug: 'url-slug-latinicej'
lang: 'ru'
category: 'goroda' # dostoprimechatelnosti|goroda|transport|arenda-avto|relokatsiya|strahovka|razvlecheniya|novosti
publishedAt: 2026-06-18
updatedAt: 2026-06-18
cover:
  src: '/images/url-slug-latinicej/cover.webp'
  alt: 'Осмысленное описание обложки'
coverCredit: 'Фото: Автор / источник, лицензия' # для CC/чужих фото обязательно
gallery: []
affiliate: [] # при нужде: - { partner: 'trip-hotels', label: '…', position: 'body-end' }
sources: [] # домены/ссылки первоисточников фактов
draft: true # снять в false при публикации (после зелёных гейтов)
---

Лид-абзац: прямой ответ на целевой запрос в 2–3 предложениях, без «разгона».
(H1 рендерится из `title` — здесь символ `#` НЕ пишем.)

## Первый смежный запрос (H2)

Текст по делу. Где уместно — таблица цен/расстояний/сравнения:

| Параметр | Значение |
|---|---|
| Расстояние | ~XXX км (уточняйте) |
| Время в пути | ~X ч |

<figure class="figure">
<img src="/images/url-slug-latinicej/g1.webp" alt="Что на фото" width="1280" height="853" loading="lazy" decoding="async" />
<figcaption>Подпись — что на фото. <span class="figure__credit">Фото: Автор / источник, лицензия</span></figcaption>
</figure>

## Второй смежный запрос (H2)

Полезный текст без воды.

### Уточняющий подпункт (H3)

Текст.

## Что рядом / читайте также

- Путеводитель по [связанному разделу](/ru/goroda/drugoj-slug/)
- Смежная тема: [аренда авто](/ru/arenda-avto/)
```

---

## 12. Как добавлять ЗАВЕДЕНИЕ (ресторан / услугу) — это делаю я

Заведения — это НЕ лонгрид-статьи, а карточки-директории:
- **Ресторан / кафе / бар / винотека** → коллекция `restaurants/` → раздел «Где поесть» `/eda/`.
- **Услуга** (риелтор / клининг / переезд / ремонт / документы / переводчики) → коллекция `services/` → `/relokatsiya/uslugi/`.

### Жёсткие правила отбора (обязательно)
1. **Только реальные заведения** с проверяемыми данными. Вымышленные/«примеры» в директорию проверенных мест НЕ вносим (правило 4, §15).
2. **Консенсус > 4★ из НЕСКОЛЬКИХ источников** + наш `ratingNote`/`review` своими словами (чужие отзывы дословно нельзя, §15).
3. **Фото обязательно** (`cover`; для CC — `coverCredit`). Цены/часы (`dishes`/`hours`) — только из источника, иначе не указывать.
4. **Пара ru + uk** (+en по желанию), одинаковый `slug`.
5. **🔵 Метка «Проверено» — ОБЯЗАТЕЛЬНА** (правило владельца, §13 ниже).

### Шаги
1. Скаффолд: `npm run new -- restaurant <slug> --city "Тбилиси"` (или `service <slug>`) → создаст ru/uk/en + папку фото.
2. Заполни обязательные поля. **Ресторан:** `name`, `city`, `priceLevel`, `geo.coord`, `mapUrl`, `ratingNote`, `sources`, `summary`, **`verifiedAt`**, `cover`. **Услуга:** `name`, `rubric`, `summary`, реальный `url`/`contact` (без example.com), **`verifiedAt`**.
3. Фото (§6); `verifiedAt` = дата нашей **реальной** сверки данных.
4. Перевод uk (+en) → снять `draft` → `npm run qa` = GO → коммит пары.

### Добавить заведение в витрину на главной (с флагом города)
В `HomePage.astro` → `showcasePicks` добавить pick с полем `city` (`city: 'Тбилиси'`) → на карточке появится пин-чип «📍 Город» (см. память `home-showcase-monetization`). В витрину помещаем только заведения с меткой «Проверено» (§13).

---

## 13. 🔵 Правило: метка «Проверено» обязательна у КАЖДОГО заведения/услуги

**КАЖДОЕ заведение и услуга, которые мы вносим, обязаны нести видимую метку «Проверено» (галочка ✓)** — это обещание директории «проверенные места» и ключевой сигнал доверия (E-E-A-T).

- **Чем:** единый компонент **`VerifiedBadge`** (вино-галочка ✓ + «Проверено · {дата}» + расшифровка «опытными путешественниками, гидами и местными»). Дата — из поля `verifiedAt`. **Не изобретать новых галочек** — один знак доверия на весь сайт (консистентность).
- **Честность (правило 4):** `verifiedAt` ставим ТОЛЬКО при реальной сверке (данные сверены с источниками/на месте). «Проверено» на непроверенном — запрещено.
- **Текущее состояние:** рестораны (`/eda/`) — `verifiedAt` обязателен схемой → полный бейдж «Проверено · {дата}» ✓. Услуги (`/uslugi/`) — ✅ **добавлено (2026-06-18):** поле `verifiedAt` в схеме `services` + **компактный** `VerifiedBadge` (проп `compact`: ✓ + «Проверено», без даты) в карточках; ставить только реально сверенным услугам (демо/вымышленные — без). Витрина: компактная ✓ на карточке заведения — когда заведения туда попадут.
- **Компонент:** `VerifiedBadge` теперь имеет `compact` (для плотных карточек — ✓ + «Проверено») и полный (✓ + «Проверено · {дата}» + расшифровка — для страницы места/шапки статьи).
- **Метка «Проверено» — БЕЗ даты у ВСЕХ заведений** (решение владельца 2026-06-18; даты не добавлять никогда): `RestaurantCard` всегда рендерит компактный бейдж «Проверено» без даты. `verifiedAt` остаётся обязательным в схеме (хранится для записи), но в карточке НЕ показывается.
- **Знак качества** — золотая медаль `/images/quality/seal-gold.webp` (выбор владельца; запасные SVG-печати `seal-star`/`seal-rosette`/`seal-shield`/`seal-laurel`/`seal-check` там же).
  - **ОПТ-ИН, только по запросу владельца.** Поле `qualityMark: true` у ресторанов И у статей (`articleBase`); по умолчанию выкл. **Перед созданием любого заведения — СПРОСИТЬ владельца, нужен ли знак.** Сейчас: Guinness, Teatro, SushiMan.
  - **Размер ФИКСИРОВАН и един:** карточки (витрина `.scard__quality`, `/eda/` `.rcard__quality`, миниатюра хаба `.card__quality`) — **62px**, выходит за правый верхний угол + золотое свечение; обложка-герой статьи (`.cover__quality`) — 72px, в углу. Не делать адаптивным.
  - Сменить знак везде = поменять путь в `ShowcaseRail.astro` (`QUALITY_MARK`), `RestaurantCard.astro`/`ArticleCard.astro`/`ArticlePage.astro` (`*__quality` src). Подробности — память `quality-mark-establishments`.
- **Сайт + Instagram заведения** (правило владельца 2026-06-19): **отдельные поля** `website` (личный сайт → кнопка «Сайт», `globe`) и `instagram` (→ кнопка «Instagram», icon `instagram`) — у ресторанов и статей-мест. Рендерятся в карточке `/eda/`, поповере витрины и статье. **При добавлении ЛЮБОГО заведения СПРАШИВАТЬ у владельца Instagram** (память `establishment-instagram`). **Личный сайт НЕ убирать** — показываем обе кнопки, если есть и сайт, и IG.
- **Фото заведений: карусель + лайтбокс.** Несколько фото = `cover` + `gallery[]`; в карточке листаются (стрелки на наведении/свайп, `/js/rcard-gallery.js`), клик по фото → полноэкранный просмотр с листанием (`/js/lightbox.js`). Пример — `restaurants/*/guinness-by-sinori.md` (4 фото: обложка + 3 в галерее, включая пинту Guinness).
- **Витрина на главной поддерживает заведения:** в `HomePage.astro` → `showcasePicks` добавить `{ kind: 'restaurant', slug: '<slug>', edaSlug: 'gde-poest-<город>', kicker: 'food' }` → карточка с обложкой заведения, ссылкой на страницу города в «Еде» и локализованным пин-чипом города (берётся из `city` карточки). В витрину — только заведения с меткой «Проверено».
