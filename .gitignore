# Ruflo / claude-flow local runtime artifacts (not part of the site)
.swarm/
.claude-flow/
ruvector.db
claude-flow.config.json
/data/
*.db
*.db-shm
*.db-wal

# Node / Astro (populated in R1)
node_modules/
dist/
.astro/
.output/

# Claude Code local-only (project agents/settings.json ARE committed)
.claude/settings.local.json

# Секреты — НИКОГДА не коммитить (Unsplash API ключ и т.п.)
.env
.env.*

# Content-pipeline scratch (photo candidates / manifests — never ship to dist)
.tmp-*
public/images/_scratch/
_scratch/
scripts/_*.mjs

# OS / editor
.DS_Store
Thumbs.db

# временный контакт-лист фото главной (для просмотра владельцем)
home-photos.html
