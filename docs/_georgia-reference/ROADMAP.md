# ROADMAP — доработки Georgia Guidebook (21 пункт владельца, 2026-06-17)

Источник: мульти-агентный аудит (11 кластеров, 858K токенов) против реального кода.
Каждый пункт: **что сейчас → план → файлы → агент → effort(S/M/L/XL) / приоритет(P0–P3) → блокер/решение владельца**.
Железные правила (SPEC/CLAUDE) и **новое правило «фото обязательно у всего контента»** ([[content-photos-mandatory]]) применяются ко всем пунктам.

## Сквозные находки аудита (важно)
- **Методология-самоописание дублируется 6–8×** (hero, footer, trust-блок, about, eda.lead, catalog.intro, каждое hub-intro) → канцелярит + near-duplicate-риск для SEO. Лечится в пп. 1/5/11.
- **п.9:** блока «Заметили неточность?» в статьях СЕЙЧАС НЕТ — он только на /o-sajte/. Это сборка целевой пары, не починка.
- **п.8:** статьи уже на `GoogleMap` (click-to-load), не Leaflet. Лучший путь «карта сразу» — перевести на self-hosted `MapEmbed variant=place` (Leaflet уже авто-грузится во вьюпорте через map-init.js).
- **п.15:** Google Directions Embed = Cloud-проект + ключ + **включённый биллинг** (платный SKU). Рекомендация: остаться на Leaflet и **запечь реальную дорожную геометрию (OSRM) на сборке** — бесплатно, без ключей/cookie/CSP-правок.
- **п.13:** рестораны `obrazec-*` = `draft:true` (не в рендере); у схемы restaurants НЕТ поля `demo`. Нужна модель «реалистичный, но честный пример» + правка схемы/EdaDirectory.
- **Фото-дыры:** новость-страховка (нет cover), 3 релокация-подстатьи, **22 живых ресторана /eda/**, демо-услуги/развлечения. Routes — все с cover.

---

## РЕШЕНИЯ ВЛАДЕЛЬЦА (блокируют исполнение — нужно подтвердить)
- **D1. Тексты (1,2,4,5,11):** выбрать варианты переформулировок (даны ниже) — или «бери рекомендованные».
- **D2. Trust-блок (5):** ✅ РЕШЕНО (2026-06-17): **убрать секцию целиком** (заменить 1 строкой под витриной).
- **D3. Карты в статьях (8):** ✅ РЕШЕНО: **self-hosted Leaflet/OSM авто-показ** (приватно, бесплатно, без cookie/правок CSP).
- **D4. Маршруты (15):** ✅ РЕШЕНО: **Leaflet + OSRM-геометрия на сборке** (без Google-биллинга).
- **D5. Новости (7):** позиция блока на главной (рекоменд. №2 под hero), сколько записей (3–4), окно 2 дня; политика старых новостей (рекоменд. оставить страницы в индексе, убрать из ленты); **нужен Cloudflare Deploy Hook + GitHub Secret** для ежедневного rebuild.
- **D6. Демо (13):** ✅ РЕШЕНО, затем **ПЕРЕСМОТРЕНО 2026-06-17 (`abdc320`):** метку «Пример» (бейдж + дисклеймер + клаузы в description) УБРАЛИ по решению владельца — демо-заведения показываются как обычные карточки; `noindex` + исключение из sitemap СОХРАНЕНЫ (вымышленное не индексируется как реальное). Риск проговорён. **Бейдж не возвращать.** См. [[live-site-no-dev-placeholders]].
- **D7. Казино (14) и рестораны (фото):** CC-фото настоящих казино/ресторанов почти нет — принять честные фасадные/городские/жанровые кадры или запрашивать фото у заведений (вне CC-пайплайна).
- **D8. Dev-зависимости для QA (21):** ✅ РЕШЕНО+РЕАЛИЗОВАНО (2026-06-17): разрешено и сделано — `npm run qa:browser` (mobile-Lighthouse, lighthouse+chrome-launcher в devDeps, `74df6a0`). Опц. добор (axe/pa11y/Playwright-responsive) — позже по желанию.
- **D9. Порог фото для гейта (20):** статья = cover + (≥3 инлайн ИЛИ ≥6 gallery); место/услуга/новость = cover. Ок?

---

## ВОЛНА 0 — ✅ СДЕЛАНО (коммит f183d6f, 2026-06-17)
| # | Задача | Файлы | Агент | E/P |
|---|---|---|---|---|
| 3 | Убрать кнопку «Пауза» витрины (сохранить pause по hover/focus + reduced-motion) | ShowcaseRail.astro (input/label, CSS .showcase__pause*, строка-селектор 160, reduced-motion 315–317), HomePage.astro (props pauseLabel/playLabel), i18n showcase.pause/play + types.ts | coder/осн.чат | S/P2 |
| 6 | Футер «AlKo» → «KOBTSEV» | ru/uk/en.ts `footer.creator` (стр.51), HANDOFF.md | coder/осн.чат | S/P2 |
| 10 | «Как доехать» → «Расстояние» (3 языка) | ru.ts:264 `access.heading`→«Расстояние», uk→«Відстань», en→«Distance»; AccessFrom.astro коммент | coder/осн.чат | S/P2 |
| 19 | Cover новости-страховки (фото обязательно) | obyazatelnaya-strahovka-2026.md ×3 + public/images/… | photo-researcher → осн.чат | S/P0 |
| 12 | Красочная обложка «Черепашье озеро» (кандидаты: Turtle Lake reflection / autumn series) | cherepashje-ozero-tbilisi.md ×3 + webp | photo-researcher → осн.чат | S/P2 |

## ВОЛНА 1 — Главная: копирайт + структура ✅ ЗАВЕРШЕНА
**Сделано (2026-06-17):** #1/#2/#4 — живые тексты hero/витрины/услуг (`9653d0b`); #5 — trust-блок убран → компактная строка доверия под витриной (`0de8315`).
**Сделано:** #7 блок «Новости» — главная 2 дня / раздел 10 дней + клиентский фильтр свежести (`724c1cc`); **авто-rebuild ✅ (2026-06-17)** — `.github/workflows/daily-news-rebuild.yml` (cron daily, пустой коммит → Cloudflare пересборка; Workers Builds НЕ имеет deploy hooks). Память [[news-deploy-hook-pending]].
**Сделано:** #9 парные блоки внизу статьи (AboutNote → пара одинаковых карточек, `dfc188e`); #11 интро разделов компактнее — убрана повторяющаяся методология/aff-абзац, routes 5→3 (ru/uk/en). Всё запушено.
**Вне очереди:** GA4 `G-PV88LQSD3W` подключён в BaseLayout + CSP (`e02dcbb`).
- **1. hero подзаголовок** (`home.heroSubtitle`) → выгода+доверие, ≤155 зн (это meta description).
  Варианты: (а) «Спланируйте поездку по Грузии без туристических ловушек: места отбираем по рейтингу и сверяем с гидами и местными. Обновляем регулярно.» (б) «Грузия без устаревших советов: проверенные места, маршруты и еда — отобраны по рейтингу и сверены с местными и гидами.» (в) «Что посмотреть, где поесть и как доехать — собрали и проверили за вас…». _S/P1, i18n ×3._
- **2. подпись витрины** (`home.showcase.lead`) — убрать инструкцию про курсор/паузу (кнопку убираем).
  Варианты: «Лучшие места, маршруты и наши партнёры — Грузия в одной ленте.» / «Избранное по Грузии: проверенные места, маршруты и партнёры проекта.» _M/P1 (вместе с п.3)._
- **4. услуги** (`relocation.uslugi.text`) — убрать «директория».
  Варианты: «…проверенные сервисы, чтобы не искать по чатам.» / «…проверенные исполнители без посредников.» _S/P2._
- **5. Trust-блок** — рекоменд. **удалить секцию** `.block--stone` (HomePage.astro:280–305 + CSS .trust* + ключи trust.*/badgeSampleHeading из 3 языков+types.ts); доверие уже несут hero/footer/«О проекте». Опц. замена — 1 строка-плашка под витриной со ссылкой на /o-sajte. _M/P1, чистить ключи синхронно (иначе astro check красный)._
- **11. вводные тексты разделов** — принцип: 1-й абзац только про раздел (живо, с местными названиями), методологию НЕ повторять (вынести в 1 хвост со ссылкой на /o-sajte), 1–2 абзаца вместо 3–5 (marshruty 5→2-3). Образцы для dostoprimechatelnosti/goroda/eda даны. _L/P2, i18n ×3 пачкой через en-translator._
- **9. парные блоки внизу статьи** — новый `BottomNotes.astro`: 2 одинаковых card-блока в гриде (слева AboutNote «как отбираем», справа «Заметили неточность?»→/kontakty/), мобайл — стек; подключить в ArticlePage/RoutePage/InsuranceHub/RelocationHub, на /o-sajte убрать инлайн-дубль. _M/P2._
- **7. блок «Новости» на главной + ретеншн** — отдельная секция NewsStrip (категория novosti, 3–4 строки, дата+заголовок+лид, без тяжёлых обложек, «Все новости →»), позиция №2 под hero. Свежесть SSG: **двухконтурно** — клиентский inline-фильтр по `data-published` (окна 2 дня на главной / 10 дней в /novosti/, работает всегда) + **ежедневный rebuild** (GitHub Actions cron → Cloudflare Deploy Hook) для корректного серверного списка/sitemap. Старые новости (>10 дн): убрать из ленты+sitemap, страницы оставить в индексе (CLAUDE правило 3 — URL не плодить в 404). _M/P1, нужен D5 + Deploy Hook._

## ВОЛНА 2 — Карты ✅ ЗАВЕРШЕНА (D3/D4)
**Сделано:** #8 карта в статье видна сразу — self-hosted Leaflet/OSM авто-загрузкой во вьюпорте (`3ae9ff9`); #15 — реальный маршрут по дорогам (OSRM-геометрия запечена в `src/data/route-geometry.json` скриптом `build-route-geometry.mjs`, рисует map-init.js; 9/9 маршрутов) (`8c1b40a`).
- **8. карта в статье сразу** — рекоменд.: заменить `GoogleMap` на `MapEmbed variant=place` (Leaflet авто-инициализируется во вьюпорте через map-init.js, self-host, без cookie/CSP-правок). _S/P1._
- **15. построенный маршрут по дорогам** — рекоменд.: остаться на Leaflet, запечь реальную полилинию OSRM на сборке (`scripts/build-route-geometry.mjs` + поле геометрии или Astro integration), рисовать в map-init.js, фолбэк на прямые линии. Без ключей/биллинга/CSP-правок. _L/P2._

## ВОЛНА 3 — Глубина контента + фото (D7/D9 приняты по факту)
**Сделано (2026-06-17, `13499dc`):** #14 казино — «ужасные» обложки заменены (Тбилиси → реальное казино Shangri La, Батуми → подсветка башни Sheraton, честные подписи, CC); фото-дыры relocation закрыты (cover для bank/internet-sim/dolgosrochnaya-arenda ×3, CC); гейт `check-photos` (#20c) подключён в `npm test`.
**#16/#17 ✅ (вечнозелёные гайды, 2026-06-17, `13e0775`/`bf21b7a`/`3dd04ac`):** аренда (4: kak-arendovat-avto углублён +штрафы/CDW/выезд за границу; arenda-tbilisi/arenda-batumi/bez-depozita +фото) и транспорт (6: tbilisi-aeroport +«По прилёту»; kutaisi-aeroport +лоукост-багаж; metro +«Канатки»+2 фото; tbilisi-kazbegi +фото; aeroporty-gruzii/tbilisi-batumi уже были с фото) — все ru/uk/en, CC-фото, факты «ориентир/уточняйте» (правило 4). qa → GO.
**#17 страховка ✅ (`8f30a2b`):** раздел /strahovka/ — богатый хаб (InsuranceHub, контент в i18n `dict.insurance`, не .md). Углублён: блок «Что делать при страховом случае» (claims) + 2 CC-фото (горы Сванетии + тропа к Ушбе; раздел был без фото). Покрытие/исключения/активный отдых уже были в requirements/penalty/notes. ru/uk/en, qa → GO.
**Рестораны /eda/ ✅ (2026-06-17, `6d8fe64`→`704192c`):** все 21 живой ресторан с обложкой — vino-underground реальное фото заведения (Flickr CC), 20 — жанровые фото блюд + бейдж «иллюстрация» (`coverIllustrative`) + честная подпись. **Unsplash подключён** (ключ владельца в `.env` gitignored; `unsplash-candidates.mjs`+`build-unsplash.mjs` с download-trigger) — 9 ресторанов общих тем на Unsplash (качество выше); грузинская специфика на Flickr CC/Wikimedia (`openverse-candidates.mjs`). obrazec-* draft. Память [[restaurant-photos-illustrative]].
**Мёртвый FAQ-код ✅ (2026-06-17):** удалён `FAQBlock.astro` + схема `faqItem`/поле `faq` + `faq`/`faqHeading` из i18n (ru/uk/en) и types + `faq:`-фронтматтер из 70 .md; SPEC §8.1/§9/§14 синхронизированы. check/build/test/lint зелёные.
**Раздел «Казино» расширен ✅ (2026-06-17, `248e34a`+`add3373`):** #14 углублён — выделена подкатегория `kazino` (фильтр-чип) + пиллар `kazino-v-gruzii` (ru/uk/en, выверенные факты с источниками: легальность/реформа 18-25/реестр 1,5млн/онлайн/ответственная игра) + связка пиллар↔Батуми↔Тбилиси. Casino-affiliate НЕ ставим (реклама гемблинга запрещена → trip-hotels). Реальные казино только Батуми/Тбилиси (ресёрч) — др. города не плодим. Память [[casino-section-plan]].
- **16/17. Транспорт (6) + Аренда (4) + Страховка (1)** — расширить каждую статью (нюансы перечислены по статьям в аудите: аэропорты — терминалы/ночной прилёт/Tax Free; метро — Metromoney/канатки/часы; аренда — CDW/SCDW/франшиза/граница/топливо/штрафы/зима/кресла; страховка — покрытие/исключения/активный отдых/как заявлять) + по 2 фото в каждую (новые папки public/images/{slug}/). **Факты — только «ориентир/уточняйте» или из источника (правило 4).** _per-article M–L; страховка P0 (фото обязательно сейчас)._
  Агенты: research-collector (черновая фактура с источником) → seo-content-writer → fact-checker → uk/en-translator → content-editor; photo-researcher+осн.чат.
- **14. Казино** — честные фасадные/городские кадры (CC игровых залов нет); подписи про здание/город, без выдумки. _M/P2._
- **Фото-дыры** — relocation (bank/internet-sim/dolgosrochnaya-arenda) допустимы нейтральные тематические CC; **22 ресторана /eda/** — XL, честная стратегия (CC-фасад если есть → фото от заведения → жанровое блюдо только в gallery с честной подписью → без фото как штатная деградация). _P3/XL._

## ВОЛНА 4 — Демо как настоящие ✅ ЗАВЕРШЕНА (D6, `e412bd7`) — ⚠️ ОБНОВЛЕНО 2026-06-17 (`abdc320`): метки «Пример» сняты по решению владельца (см. D6 выше; бейдж не возвращать)
**Сделано (2026-06-17):** 6 живых демо переведены в реалистичные честные примеры (вся обвязка — бейдж «Пример» + блок-дисклеймер demo-note + noindex — уже была в шаблонах ArticlePage/ServicesDirectory/ArticleCard/EntertainmentHub).
- Развлечения (3 статьи ×3 яз): «Айвани» (лаундж), «Резонанс» (клуб), «ВелоБульвар» (прокат) — CC-обложки (Kleines Phi / Tigerheat / BatumVelo), живое описание района/атмосферы, без выдуманных фактов. demo:true → noindex + demo-note + бейдж.
- Услуги (3 карточки ×3 яз): «Светлый дом», «Картули», «Вере Хоум» — убран example.com (url/contact), реалистичные summary; у zhilyo снят ложный sponsored (золотая рамка = только реальный платный партнёр). Каталог услуг noindex, пока нет реальных сервисов (`hasRealItems=false`).
- Рестораны `obrazec-*` оставлены **draft** (шаблоны фронтматтера, не в рендере) — фиктивные «места» в директорию проверенной еды не подмешиваем.
- **Нюанс к памяти [[live-site-no-dev-placeholders]]:** D6 уточняет правило — демо допустимы как ОТПОЛИРОВАННЫЕ честные примеры (имя+фото+бейдж+дисклеймер+noindex), но НЕ как сырые заглушки («(пример карточки)»/example.com).
- **13. realistic-but-honest sample** — единая модель: вымышленное правдоподобное имя (без «образец» в названии), без проверяемых фактов (адрес=район, без точных цен/часов/телефона), CC-фото, ненавязчивый бейдж «Пример» + дисклеймер + noindex. Реализация: добавить `demo` в схему restaurants + бейдж в EdaDirectory; обновить services primer-* (убрать example.com) и развлечения demo. Затем синхрон доков (SPEC/CLAUDE/AUDIT). _M–L/P1–P2._

## ВОЛНА 5 — Система наполнения ✅ ЗАВЕРШЕНА (пункт 20, `031a3bd`)
- **20a** ✅ DoD-чеклисты по типам (статья/маршрут/место/услуга/город) — в CLAUDE.md (раздел «Definition of Done» + конвейер); полный список вшит в скелеты. _(ROADMAP говорил «SPEC §12», но §12 = i18n — положено в CLAUDE.md, корректный дом для рабочих правил.)_
- **20b** ✅ `scripts/new-content.mjs` + `npm run new` — скаффолдер ru/uk/en (схема-корректный frontmatter, cover обязателен, DoD-чеклист в комментарии, draft:true, не перезатирает); типы article/news/route/restaurant/service/city.
- **20c** ✅ `scripts/check-photos.mjs` — гейт «нет cover → красная сборка» (реализация правила фото), в `npm test` (`13499dc`). Статья/маршрут без cover валит сборку; рестораны/услуги/города — WARN (беклог 63).
- **20d** ✅ skill `.claude/skills/add-content` — тонкий оркестратор (бриф→скелет→writer→фото→переводы→fact-check→content-editor→гейты→коммит→синхрон доков).
- **20e** ✅ промпты агентов синхронизированы: убран мёртвый FAQ/FAQPage (seo-content-writer/seo-engineer/tester), добавлено «фото обязательны» + порядок fact-check→translate→editor + en в паре.

## ВОЛНА 6 — Финальный QA «100% покрытие» ✅ ОСНОВНОЕ СДЕЛАНО (пункт 21, `e7fbdb1`)
**Сделано:** оркестратор `npm run qa` (`scripts/qa.mjs`) → чистая сборка + гейты (check/build/test/links/lint) + аудит dist/: SEO (title≤60/desc/canonical/og, с декодом сущностей), взаимность hreflang (+x-default), schema.org JSON-LD (форма @graph), доступность (alt: нет→critical/пустой→minor; один h1), перф-бюджет (JS≤50КБ/стр; картинки ≤200КБ только реально подключённые), render-smoke (нет TODO/undefined/[object Object]), безопасность (CSP без unsafe-inline; нет inline on*=/javascript:). Отчёт critical/medium/minor + **GO/NO-GO**. **Текущий вердикт: GO** (0 critical/0 medium, 321 минор = декоративные пустые alt, by design).
**Находка QA исправлена:** hero отдавал webp до 2400px (314КБ > §15) → cap 1600px + `width:1600` у фолбэк-src; теперь ≤163КБ (выигрыш LCP).
**Браузерный слой ✅ (D8, `74df6a0`+`dc19a10`):** `npm run qa:browser` = Lighthouse + responsive.
- `qa-lighthouse.mjs`: mobile-Lighthouse по 5 шаблонам; a11y/seo/best-practices ≥90 ХАРД, perf WARN. Прогон 90–100 (GO), perf 76–94.
- `qa-responsive.mjs` (`npm run qa:responsive`): **Playwright на системном Chrome** (channel:chrome, без скачивания Chromium; playwright-core) — scrollWidth≤clientWidth на 8 шаблонах × 5 ширин (320–1280) + элемент-виновник. Нашёл и починил реальные переливы: попап погоды RatesBar (закрытый details распирал), вертик. фото страховки (max-width без min()), длинное слово в H1, длинный URL источников. GO.
- `npm audit` в `qa`: prod high/critical = medium-инфо (esbuild/vite build-tooling Astro, не в прод-статике, фикс мажором), GO не блокирует.
- **check-interlinks ✅ (2026-06-17):** `scripts/check-interlinks.mjs` в гейте `npm test` — DoD «≥2 внутр. ссылки в теле» (markdown+raw `<a>`, без `/go/`, дедуп; articles+routes, draft скип). 243/243 OK (дочинена страховка-2026 ×3: +ссылка на аренду). WARN-режим (как check-photos), `--strict` валит.
- _Опц. остаток:_ axe/pa11y отдельно — **избыточно** (a11y Lighthouse в `qa:browser` уже на axe-core, включая контраст).
**Опциональные агенты (по желанию владельца):** `accessibility-auditor` (в значит. части покрыт статикой qa) — добивает рантайм-контраст/ARIA при D8; `research-collector` — черновая травел-фактура с источником в `briefs/` (разблокирует факты казино/новостей для Волны 3 #16/#17).
- Прежний план (скрипты/агенты) — см. историю ниже.
- check-interlinks ✅ добавлен (см. выше). check-redirects — **уже покрыт** `check-links.mjs` (резолв внутр. путей + сверка `/go/{partner}` с partners.json). check-contrast (токены §10) — низкий ROI: рантайм-контраст ловит axe в `qa:browser`, а проблема была не в контрасте, а в несуществующих токенах (`--space-5/-7`, [[visual-check-and-space-tokens]]); добирать при необходимости.

---

## БУДУЩИЕ КРУПНЫЕ ДОРАБОТКИ (за рамками 21 пункта)

### Переделать фоновые картинки главной — ✅ СДЕЛАНО (2026-06-19)
**Задача владельца (2026-06-19):** текущий фон/картинки главной выглядят **некачественно и блекло** — переделать.
**Сделано (2026-06-19):** собрал подборку ~20 ярких кадров Unsplash на одобрение → владелец отверг #1–4, остальное разрешил. Заменил весь набор на кадры Unsplash; позже владелец попросил убрать Батуми (высотки Орби). Итог — **5 кадров**: gergeti-kazbek-2 (Deogyeon Hwang, дефолт), ushguli (Patrick Schneider), mestia-ushba (Mike Swigunski), tbilisi-colorful (Inna Nasonova), kakheti-vineyards (Giga Goriashvili). Ротация каждые 3ч сохранена ([[home-hero-rotation]]).
- **Перф:** исходники 2000px, `HERO_WIDTHS=[640,960,1280]` (кап 1280, НЕ 1600/2400 — иначе >200КБ); каждый вариант ≤200КБ (gergeti 1280=198КБ впритык). `npm run qa` = GO.

### Авто-агрегатор новостей — скилл `/news` ✅ РЕАЛИЗОВАН (2026-06-19)
**Задумка владельца (2026-06-17):** по команде собрать новости с источников, обработать и выложить. Главная боль — **мусор**.
**Сделано (2026-06-19):** скилл `.claude/skills/news/SKILL.md` (коммит bf686cd); источники подтверждены владельцем; **первый прогон** (коммит 414f10b) опубликовал 2 новости — `georgian-airways-tbilisi-batumi`, `letniy-festival-batumi-2026`. Фаза 2 (автопилот по расписанию: GitHub Action + Claude через API) — НЕ начата.

**Развилки закрыты владельцем (2026-06-19):**
- **Модель публикации:** команда `/news` → я готовлю батч → **превью владельцу → «ок» → публикация** (НЕ слепой автопостинг; по сути 1 команда + «ок»).
- **Тематика (= фильтр мусора):** **только тревел-релевантное** — въезд/визы/границы, авиа/рейсы/аэропорты, цены/валюта/налоги для туриста, погода/ЧС/безопасность, открытия-закрытия мест и транспорта, крупные события/фестивали. Политика/криминал/спорт/светская хроника/реклама/кликбейт — отсев.
- **Ритм/объём:** **дайджест за последние 24 ч, топ 3–5**. Запускает владелец (раз в день или по желанию); полный автопилот «по расписанию без участия» = фаза 2 (GitHub Action + вызов Claude через API, отдельная инфра/бюджет).
- **Реализация:** **Claude Code skill**, я гоняю конвейер своими инструментами (WebSearch/WebFetch + фото-пайплайн Unsplash/Commons + переводчики ru→uk→en + `qa`). Не headless-скрипт без LLM.

**Источники (стартовый список, владелец подтверждает/дополняет):** Agenda.ge, Civil.ge, OC Media, DFWatch, Georgia Today, Interpressnews (EN); Google News RSS по запросам «Georgia travel / airport (Tbilisi/Kutaisi/Batumi) / visa / border / tourism»; офиц. — аэропорты (TAV), Georgian Airways, МВД/погранслужба, Нацбанк (курс).

**Конвейер скилла:** сбор (24ч) → фильтр мусора (релевантность тревел-нише + дедуп/кластеризация + анти-кликбейт/реклама; финальный судья — LLM, не регэкспы) → рерайт своими словами с `sources` → фото → перевод ru/uk/en → черновики (`draft:true`) → превью-дайджест владельцу → «ок» → снять draft → `qa` GO → коммит+пуш. Свежесть/ретеншн уже есть ([[news-deploy-hook-pending]]: 10-дн окно + ежедневный rebuild).

**Железные правила при реализации (иначе риск бана/искажений):**
- **Правило 4 (факты не выдумывать):** LLM-обработка обязана РЕРАЙТИТЬ факты с сохранением источника, без галлюцинаций; каждая новость — со ссылкой на первоисточник (как в существующих новостях, поле `sources`).
- **Копирайт/ToS (§15):** чужие новости НЕ копировать дословно (та же логика, что с чужими рейтингами) — только свой пересказ + ссылка.
- **Фильтр мусора (главная боль владельца):** жёсткий отсев по релевантности (тема — Грузия/тревел/наша ниша), дедуп одной новости из многих источников, отсев рекламы/кликбейта.
- **Фото обязательно у каждой новости** ([[content-photos-mandatory]]) — авто-подбор легального фото (Openverse/Unsplash, см. [[restaurant-photos-illustrative]]) или штатная деградация.
- **Пара ru+uk (+en):** авто-перевод по нашему пайплайну переводчиков.
- **НЕ слепая автопубликация:** «1 клик» = собрать → отфильтровать → подготовить ЧЕРНОВИКИ (`draft:true`), владелец просматривает и одобряет (live-site качество, [[live-site-no-dev-placeholders]]); публикация — отдельным действием. Свежесть/ретеншн уже есть ([[news-deploy-hook-pending]]: 10-дн окно + ежедневный rebuild).

**Возможный подход (наброски, НЕ решение):** источники — RSS грузинских изданий + Google News RSS по ключевым запросам + офиц. каналы; окно 4ч; кластеризация+дедуп; LLM рерайт+перевод+фактчек против источника; подбор фото; генерация `.md` черновиков в `briefs/` или `draft:true`; превью владельцу → одобрение → коммит. Effort: XL (отдельный проект). Разблокирует/связан с агентом `research-collector`.

---

## АГЕНТЫ
**Приоритет — проектные** (заточены под правила сайта): seo-strategist, seo-content-writer, fact-checker, uk-translator, en-translator, content-editor, astro-platform-engineer, coder, architect, tester, reviewer, security-auditor, seo-engineer, photo-researcher, monetization-strategist, **research-collector, accessibility-auditor, perf-diagnostician**. Skill: ui-ux-pro-max (только Tailwind/HTML-CSS режим, игнорировать React-вывод).

**Новые агенты ✅ созданы (2026-06-17, файлы в `.claude/agents/`) — ТЗ реализованы:**
1. **research-collector** (P2) — собирает ЧЕРНОВУЮ травел-фактуру (адреса/часы/цены/события казино, развлечений, новостей) из офиц. источников с ОБЯЗАТЕЛЬНОЙ ссылкой → `briefs/`; не публикует, не выдумывает (правило 4). Разблокирует контент казино/развлечений/новостей.
2. **accessibility-auditor** (P2) — WCAG-аудит против токенов §10 (контраст ≥4.5, focus-visible, alt/ARIA, prefers-reduced-motion в hero/анимациях, клавиатура).
3. **perf-diagnostician** (опц., P3) — диагностика деградации §15 после крупных правок (можно заменить разовым my_agents perf-analyzer).

**my_agents (форк ruflo) — точечно, как консультанты под надзором проектного reviewer:** frontend-specialist (сложный нативный JS, игнорировать React), accessibility-specialist (разово до своего), perf-analyzer (диагностика), skill `/analyze-project` (P1/P2/P3 аудит для приоритизации), `ruflo-security-audit:dependency-check` (доп. CVE). **НЕ брать:** generic content/seo/writer (выдумают факты — правило 4), MCP-память ruflo (нестабильна на Windows, см. [[ruflo-agents-caveats]]).

---
_Полный детальный аудит (currentState по строкам, все варианты, блокеры) — в транскрипте воркфлоу wf_dac5fbbc-ee4 / `_scratch/audit-digest.txt`._
