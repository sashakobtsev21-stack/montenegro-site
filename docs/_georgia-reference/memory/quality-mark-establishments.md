---
name: quality-mark-establishments
description: ✅ ЗНАК КАЧЕСТВА = золотая медаль `seal-gold.webp`; ОПТ-ИН, только у избранных (Guinness/Teatro/SushiMan), СПРАШИВАТЬ владельца перед добавлением к новому заведению
metadata:
  type: feedback
---

**⚠️ ГЛАВНОЕ ПРАВИЛО (владелец, 2026-06-18):** знак качества — НЕ у всех, только у **избранных** мест. Ставится **ТОЛЬКО по явному запросу владельца**. **Перед созданием ЛЮБОГО заведения СПРОСИ у владельца, нужен ли знак качества.** Технически — поле `qualityMark: true` (рестораны) / `quality: true` (пик витрины); по умолчанию выключено. Сейчас знак стоит у **3 мест: Guinness by Sinori (паб), Teatro (клуб), SushiMan (доставка суши)**. Знак выходит за правый верхний угол карточки (и в /eda/, и в витрине) + золотое свечение.

**✅ ТЕКУЩИЙ ЗНАК (2026-06-19, Midjourney):** владелец сгенерил медальон **звезда + лавровый венок + лента** (золото, на БЕЛОМ фоне) → **`/images/quality/seal-gold.webp`** (~42 КБ, прозрачный). Обработка: вырез белого фона по десатурации (`sat<20 && mn>=250` → alpha 0, фезеринг 236..250), вкл. просветы кольца; trim + resize 256; node+sharp инлайн (читать исходник через fs.readFileSync — sharp не пишет в тот же файл). **Размер ФИКСИРОВАН:** карточки (витрина/`/eda/`/миниатюры хаба) — 62px, вынос за угол + золотое drop-shadow; герой статьи — 72px. Путь один везде — сменить знак = заменить файл по этому пути. История: до этого PNG-медаль на зелёном (хромакей), ещё раньше SVG-печати (запас `seal-*.svg`).

**Запасная подборка (v2):** собственные SVG-печати в фирменных цветах (вино `#6e2639` + золотой градиент `#F7DF9A→#D8B05A→#A67E2E`) в **`public/images/quality/`** — на случай если владелец передумает:
- `seal-star.svg` — золотое кольцо + вино + золотая звезда;
- `seal-rosette.svg` — наградная розетка-санберст + лентами (самый «официальный/наградной»);
- `seal-shield.svg` — герб-щит + звезда + лента;
- `seal-laurel.svg` — вино-диск + лавры + звезда;
- `seal-check.svg` — кольцо + кремовый центр + вино-галочка.

Подборка-контактный лист: `_scratch/dz/seals-contact-sheet.png` (рендер всех 5).

Где показывается:
- **витрина на главной** — `.scard__quality` в `ShowcaseRail.astro` (54px, **выходит ЗА рамку** top:-16/right:-14 + золотое drop-shadow свечение);
- **директория «Еда» `/eda/`** — `.rcard__quality` в `RestaurantCard.astro` (48px, в углу обложки + золотое свечение).
`alt=""` (декоративный — смысл несёт текстовая метка «Проверено»; даёт минорный a11y-варн в qa, by design).

**Это НЕ `VerifiedBadge`** («Проверено» — текстовая вино-галочка, БЕЗ даты, см. [[verified-mark-mandatory]]) — отдельная графическая эмблема. Сочетаются: печать на фото + «Проверено» в подвале карточки.

См. [[home-showcase-monetization]] (витрина), [[verified-mark-mandatory]].
