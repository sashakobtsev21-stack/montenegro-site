# AUDIT — montenegro-site (2026-06-22)

Технический аудит Этапа 1. Метод: **сначала верификация фактического состояния репо, потом вывод**. Прогоны: `npm run build` / `check` / `lint` / `test` / `qa` / `qa:browser` (lighthouse+responsive). Скилл `/full-audit` в окружении **недоступен** (`Unknown skill`; файл `.claude/skills/full-audit/SKILL.md` присутствует, но как скилл не зарегистрирован) — аудит выполнен прямыми прогонами + ручной верификацией.

**Сводный вердикт:** `npm run qa` = **GO** (0 критических, 0 средних в дист-аудите; 76 «минорных» — ложные срабатывания, см. ниже). Сборка 70 страниц, `check` 0 ошибок. Реальных Critical/High дефектов не найдено.

---

## Повторная верификация (2026-06-22, фрешем — по запросу владельца)
Полный повторный прогон с нуля — всё подтверждено:
- Гейты: `build` 70 страниц · `check` 0 ошибок/0 warning · `lint` чисто · `test` (enums/parity/photos/interlinks) все OK · **`qa` = GO** (0 крит/0 сред/76 минор-ложные). `npm audit`: prod **2 low**, dev **24** (2 low + 22 moderate) — high/critical нет.
- **Живой тест `/go/` (боевой воркер на проде) — open-redirect-защита подтверждена:** `trip-tours` → `302` на trip.com c `kwd=Montenegro`; **неизвестный `evil-unknown` → `302` на `https://montenegroguidebook.com/`** (фолбэк на свой сайт, НЕ наружу); болванка `discovercars` → discovercars.com (без атрибуции, ожидаемо); `/go/` в `dist/sitemap-0.xml` — **0 вхождений**.
- **Lighthouse mobile (фреш):** a11y / best-practices / SEO = **100** на всех проверенных; perf `/cities/`,`/car-rental/`,`/routes/` = 97, `/food/` = 95, главная **86** (hero-LCP, лабораторно вариативно — в прошлом прогоне 89). Responsive — переполнения нет (8 шаблонов × 5 ширин).
- Грузинских следов в боевом коде/ассетах — нет (grep); совпадения только в доках = намеренный провенанс / указатели на архив.

## Гейты (факт)
| Прогон | Результат |
|---|---|
| `npm run build` | ✅ 70 страниц, без ошибок/warning |
| `npm run check` (astro) | ✅ 0 ошибок, 0 warning, 2 hint (в служебных скриптах) |
| `npm run lint` | ✅ чисто |
| `npm run test` (enums/parity/photos/interlinks/links) | ✅ все гейты зелёные |
| `npm run qa` | ✅ **GO** — критич. 0 · сред. 0 · минор. 76 |
| `npm run qa:browser` (lighthouse+responsive) | ✅ см. ниже |

## Lighthouse (mobile, лабораторно)
| Страница | perf | a11y | best-pract | seo |
|---|---|---|---|---|
| / (главная) | **89** | 100 | 100 | 100 |
| /cities/ | 97 | 100 | 100 | 100 |
| /car-rental/ | 97 | 100 | 100 | 100 |
| /food/ | 95 | 100 | 100 | 100 |
| /routes/ | 97 | 100 | 100 | 100 |

Responsive: горизонтального переполнения нет (8 шаблонов × 5 ширин 320–1280).

---

## Находки по доменам (severity)

### Безопасность — ✅ OK
- **CSP** (на проде, отдаётся Cloudflare): `default-src 'self'`; `script-src 'self' googletagmanager`; `connect/img google-analytics`; `frame-ancestors 'none'`; `object-src 'none'`. Без `script 'unsafe-inline'`. **OK.**
- **Заголовки:** HSTS (preload), X-Frame-Options DENY, Referrer-Policy, Permissions-Policy, X-Content-Type-Options nosniff — все присутствуют на проде. **OK.**
- **`/go/` open-redirect:** `worker/index.ts` — только https-target, неизвестный партнёр/невалидный target → 302 на свой сайт (анти-open-redirect), `allowSubId` учитывается. **OK.**
- **Секреты:** в репо нет секретов; partner-ID в `partners.json` — публичные affiliate-ID (не секрет). GA Measurement ID — публичный. **OK.**
- **[Low] `npm audit`:** prod — high/critical нет (2 low, astro); dev — 24 (только tooling, не в прод-бандл). *Мониторить квартально.*

### Тех-SEO — ✅ OK
- hreflang `en/ru/uk` + `x-default`, `canonical`, JSON-LD (Article/BreadcrumbList/Organization/WebSite) — присутствуют на статьях. **OK.**
- `sitemap-index.xml` + `sitemap-0.xml` (66 URL × 3 языка, lastmod, `/go/` и demo исключены), `robots.txt` Disallow `/go/`. Карта живая (HTTP 200) на проде. **OK.**

### Доступность (WCAG) — ✅ OK (Lighthouse a11y = 100)
- **[ложные срабатывания]** 76 «минорных» из дист-аудита `qa` = декоративный `alt=""` у флага (корректно, `aria-hidden`) на каждой странице + hero `__bg`. Lighthouse a11y=100 подтверждает отсутствие реальных нарушений. **Чинить нечего.**

### Перформанс
- **[Medium] Главная perf 89** (<90) — узкое место hero-LCP-картинка. Лабораторный скор машинозависим; остальные страницы 95–97. *Опц. фикс: `preload` hero-изображения (вероятный +2–3).* В `ROADMAP-FIX`.
- **[ложное] hero `<img>` без width/height** — CLS не возникает: `position:absolute; inset:0` + контейнер `.hero` резервирует высоту `min-height: clamp(440px,72vh,660px)`. **Не баг.**
- Изображения статей: все webp ≤200 КБ (гейт `check-photos`). **OK.**

### Инфраструктура `/go/` — ✅ OK (вопреки исходному предположению ТЗ)
- `worker/index.ts`, `wrangler.jsonc`, `src/data/partners.json` — **СУЩЕСТВУЮТ и работают**; `astro.config.mjs` исключает `/go/` из sitemap. Проверено вживую: `/go/trip-tours?c=durmitor-national-park` → 302 на Trip.com с `kwd=Montenegro`. **OK.**
- **[Medium] EKTA** — временный прямой URL без атрибуции (клики CIS-страховки не засчитываются). Болванки EN-партнёрок (discovercars/getyourguide/booking) — прямой URL без атрибуции до регистрации. → `ROADMAP-FIX`, действие владельца.

### Контент
- 5 статей × 3 языка (`montenegro-travel-guide`, `best-time-to-visit-montenegro`, `renting-a-car-in-montenegro`, `cost-of-living-in-montenegro`, `durmitor-national-park`); коллекции `cities`/`routes`/`restaurants`/`services` — пусты (по дизайну). **OK для стадии.**
- **[Medium, не Этап 1] Фото:** у 5 статей `cover + 3` инлайн (= 4). Новая норма — **≥5**. Добор — в `ROADMAP-FIX` (Этап 1 фото не наполняет).

### Грузинские следы — ✅ вычищено
- В боевом коде грузинских следов не осталось (проверено grep). Осталась намеренная строка-провенанс «форк движка Georgia Guidebook» (SPEC/README/content.config.ts:6) — оставлена осознанно. Архив `docs/_georgia-reference/` — не трогаем.
- Скилл `/news`, `.claude/agents|skills`, build-скрипты, favicon, hero/og, enums, i18n — локализованы под Черногорию.

### UI/UX — ✅ OK
- Переключатель языка — в выпадающем меню (+ подвал); навигация десктоп/мобайл, карта (Leaflet, click-to-load), виджеты (Trip.com click-to-load) — без поломок; responsive чист.

---

## Что НЕ закрыто на Этапе 1 (см. `ROADMAP-FIX.md`)
Photo-добор (≥5) у 5 статей · перф главной (опц. hero-preload) · EKTA/EN-партнёрки (регистрация владельца) · дальнейший контент. **Hero/og и домен — НЕ pending: уже черногорские / куплен и онлайн** (исходные предположения ТЗ устарели).
