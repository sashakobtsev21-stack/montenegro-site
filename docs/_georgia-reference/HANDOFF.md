# HANDOFF — Georgia Guidebook (снимок для продолжения в новом чате)

_Снимок: 2026-06-20. Самодостаточный навигатор: открыв проект в новом чате, прочитай
этот файл + `README.md` + `CLAUDE.md` + `SPEC.md` + `PROGRESS.md` + `docs/memory/MEMORY.md`
и продолжай. Источник правды по продукту — `SPEC.md`; правила работы — `CLAUDE.md`;
детальный статус/история — `PROGRESS.md`; план доработок — `ROADMAP.md`._

---

## 0. Что это и где что лежит

- **Проект:** контентный сайт-справочник по Грузии, 3 языка (**en — язык по умолчанию, корень `/`**;
  ru → `/ru/`; uk → `/uk/`). Astro 6 (SSG, без React) + Tailwind 4 + Content Collections.
  Хостинг — **Cloudflare Workers (Static Assets)**; **деплой = push в `main`** (автосборка `npm run build`).
  Репозиторий: github.com/sashakobtsev21-stack/gruzia-site.
- **Документы:** `README.md` (точка входа), `SPEC.md` (продукт, v3.4), `CLAUDE.md` (правила),
  `PROGRESS.md` (статус+история), `HANDOFF.md` (этот), `ROADMAP.md` (план 21 правки + волны),
  `AUDIT.md` (единый сводный аудит: журнал заходов 2026-06-15…06-20 + статус устранения + сильные стороны + перф),
  `SETUP-GUIDE.md` (внешние агенты/плагины).
- **Память:** оригинал — `C:\Users\Oleksandr\.claude\projects\C--Users-Oleksandr-Desktop-GeorgiaTravel\memory\`;
  зеркало в репозитории — `docs/memory/` (переезжает с `git clone`). Индекс — `MEMORY.md`.
- **Контент:** `src/content/{articles,routes,restaurants,services}/{ru,uk,en}/*.md` — парами/тройками,
  одинаковый slug. Брифы — `briefs/{slug}.md` (на сайт не попадают).
- **Скрипты:** скаффолдер `new-content.mjs` (`npm run new`); фото — `commons-candidates`/`openverse-candidates`/
  `unsplash-candidates` + `build-gallery`/`build-unsplash`; адаптивные обложки — `build-cover-variants.mjs`
  (`npm run build:covers`); гейты — `check-*.mjs`; QA — `qa.mjs`/`qa-lighthouse.mjs`/`qa-responsive.mjs`.
- **Перед коммитом:** `npm run qa` → **GO** (чистит dist, гоняет check/build/test/links/lint + статический аудит).
- **Проектные агенты** (`.claude/agents/`): seo-strategist, seo-content-writer, fact-checker, uk-translator,
  en-translator, content-editor, architect, coder, reviewer, tester, security-auditor, seo-engineer,
  astro-platform-engineer, monetization-strategist, photo-researcher, **research-collector**,
  **accessibility-auditor**, **perf-diagnostician**, **ui-ux-auditor**. Скиллы: `add-content`, `full-audit`.

---

## 1. Текущий статус (2026-06-20)

- **Новая статья «Сколько стоит поездка в Грузию на двоих» ✅ (2026-06-20)** — скилл `/new-article`: тройка ru/uk/en, slug `skolko-stoit-poezdka-v-gruziyu`, кластер «Перед поездкой» (MONEY), `yearInTitle`. Факты идентичны (веб-ресёрч 2026: бюджет день/неделя на двоих, жильё/еда/транспорт/туры/обязательные траты), не калька. 3 своих фото под slug (хачапури/рынок/Старый Тбилиси, ≤200КБ), 9 внутр. ссылок, 3 AffiliateBox через `/go/`. `npm run qa` → GO. Опубликовано после «ок».

- **UX/UI/читаемость-аудит + полное устранение ✅ (2026-06-20)** — 8 аспектов, 65 находок → 23 темы; реализуемое закрыто полностью (все high/medium/low): таксономия кухни `cuisineKey` 25→9, уникальные заголовки городов, блок «Разделы» на главной, контраст hero, тач-таргеты 44px, маршрут-реордер, монетизация-дедуп, back-to-top, ☰ «Menu», топ-история новостей, подписи галерей, перф (Cache-Control + font-preload) и др. Единый отчёт — `AUDIT.md`. **Новое правило №9 (CLAUDE.md):** доки синхронизировать после каждой доработки. ~22 коммита `c74355c`…; qa/qa:responsive/Lighthouse зелёные.

- **Скилл `/news` (дайджест новостей) + первый прогон (2026-06-19):** реализован
  `.claude/skills/news/SKILL.md` — команда `/news`: сбор за сутки с согласованных
  источников → 3-ступенчатый фильтр мусора (релевантность/дедуп/анти-кликбейт) →
  рерайт с `sources` → фото → перевод ru/uk/en → превью владельцу → «ок» → публикация.
  Флип `draft:false` блокируется авто-классификатором до явного «ок» (барьер публикации).
  Первый прогон опубликовал 2 новости: `georgian-airways-tbilisi-batumi`,
  `letniy-festival-batumi-2026`. Фаза 2 (автопилот по расписанию) — не начата.
  Память [[news-aggregator-idea]]. Добавлено 2 новости (×3 языка).

- **Фон главной переделан (2026-06-19):** владелец забраковал старый фон («тускло/некачественно»).
  Собрал подборку ~20 ярких Unsplash на одобрение → владелец отверг #1–4, остальное разрешил;
  позже попросил убрать Батуми (высотки Орби). Новый набор — **5 кадров Unsplash**
  (gergeti-kazbek-2 дефолт, ushguli, mestia-ushba, tbilisi-colorful, kakheti-vineyards),
  ротация 3ч сохранена. Перф: исходники
  2000px, `HERO_WIDTHS=[640,960,1280]` (кап 1280 — иначе >200КБ), каждый вариант ≤200КБ.
  `npm run qa` = GO. Память [[home-hero-rotation]], ROADMAP закрыт.

- **Знак качества / Teatro place-карточка / сайт-ссылки (2026-06-19, 2-й заход):**
  знак качества **фиксированный размер** — карточки (витрина/`.eda`/миниатюра хаба) 62px,
  герой статьи 72px (зафиксировано в CONTENT_GUIDE); опт-ин у ресторанов И статей
  (`qualityMark`), вынос за рамку. **Teatro** — обложка=логотип, в витрине place-карточка
  с поповером (адрес/часы/карта/сайт) + карусель + медаль; знак на странице и в хабе
  «Развлечения». **Поле `website`** (ресторан+статья) → кнопка «Сайт» (globe) в карточке,
  поповере витрины и статье; Teatro=teatro.ge, SushiMan=sushiman.ge. Витрина — заведения
  вперемешку со статьями. SushiMan без «веранды»; Guinness g3 перекадрирован. Тексты
  /eda/ и Развлечений переписаны человечнее. **Отложено:** карусель в миниатюре хаба (#8).
  **Нужны от владельца:** сайт/IG для Guinness; доп. фото SushiMan.

- **Знак качества + новые заведения (2026-06-19):** знак качества стал **ОПТ-ИН** —
  поле `qualityMark` у ресторанов (по умолчанию выкл.), `quality` у пиков витрины;
  ставится **только по запросу владельца** (сейчас: Guinness, Teatro, SushiMan).
  **Перед созданием заведения — спрашивать у владельца про знак** (память
  `quality-mark-establishments`). Медаль = `seal-gold.webp` (готовая золотая, хромакей),
  +15%, в `/eda/` выходит за рамку (у `.rcard` убран overflow). **Teatro** — статья
  Развлечения/Ночная жизнь (Батуми) ru/uk/en + в витрину; **SushiMan** — карточка
  `/eda/` Батуми ru/uk/en + в витрину (вместо Боржоми). Фавикон вкладки → флаг Грузии.
  Раздел «Планирование» → «Перед поездкой» (URL `/planirovanie/` тот же). ROADMAP:
  переделать фоновые картинки главной (позже).

- **Витрина v3 / знак качества / deeplink (2026-06-18):** витрина заведений на главной — описание
  ВЫПАДАЕТ ПОД карточкой (резерв 168px, не срезается), стрелки фото на наведении, **знак качества
  выходит за рамку + золотое свечение**, усилено hover-свечение + затемнение соседей, клик по фото → лайтбокс.
  **Фикс drag** «не крутится, когда держишь за карточку» — глушим нативный `dragstart` (рвал прокрутку).
  В `/eda/` стрелки прозрачные. **Знак качества = золотая медаль `seal-gold.webp`** (выбор владельца,
  хромакей зелёного фона; запасные SVG-печати в `public/images/quality/`). **Клик из витрины →
  центрирование+подсветка**: ссылка `/eda/{город}/#r-{slug}` → `card-deeplink.js` центрирует карточку,
  золотая подсветка + затемнение остальных (`anchorId` в RestaurantCard). **Заголовки city-страниц**
  → «Рестораны и кафе {город}» (было «Где поесть в {город}»; ru/uk/en). Паб `guinness-by-sinori` +4-я фотка.
  См. память `quality-mark-establishments`, `home-showcase-monetization`.
- **Последний коммит:** `0a11a8d` (на момент снимка; актуальный — `git log -1`). **Сайт: 298 страниц.**
- **Качество:** `npm run qa` → **GO ✅** (0 critical / 0 medium); `qa:browser` → Lighthouse a11y/SEO/best-practices
  **≥90 везде**, perf 80–93 (лаб-скор, шумный); адаптив — 0 переполнений (320–1280). Кастомная 404 на месте.
- **English — язык по умолчанию ✅:** en на корне, ru `/ru/`, uk `/uk/`; весь корпус переведён. Архитектура —
  память `english-3rd-language`. x-default → en. `ka` (грузинский) — НЕ делаем (решение владельца).
- **21 правка владельца (`ROADMAP.md`) — все по сути закрыты** (волны 0–6): тексты главной, карты (Leaflet/OSRM),
  демо как честные примеры, система наполнения (`npm run new` + DoD + skill `add-content`), QA-оркестратор.
- **Полный мультиагентный аудит ✅ (2026-06-17)** — `AUDIT.md` (~73/100, 103 находки).
  Фиксы по нему — волны A–G (см. §8 отчёта и `PROGRESS.md`):
  - demo вне sitemap + `noindex,nofollow`; CI = полный `npm test`; a11y-контраст/фокус (токены
    `--border-cta`/`--color-gold-text`); meta-description по границе слова (`clampDesc`);
    worker https-only + `allowSubId` + HSTS; **srcset обложек** (`build:covers`, главный perf-рычаг);
    reveal-страховка; кластер **«Страховки»** (гайд `kak-vybrat-strahovku`, SafetyWing, **без EKTA**);
    JSDoc route-файлов, ARIA-дедуп, дедуп «Боржоми»; **кастомная 404** (+ флаг BaseLayout `utility`).
  - Аналитика = **GA4** (решение владельца).

**Осталось:**
- **Действие владельца:** отключить **Cloudflare Web Analytics** в дашборде (сейчас параллельно с GA4 —
  «одна аналитика», правило 8), затем убрать `cloudflareinsights.com` из CSP (`public/_headers`).
- **Решения владельца (§7 аудита):** унификация маркёров платного (бейдж «Партнёр» vs золотая рамка),
  судьба мёртвой коллекции `cities`, ширина колонки чтения на десктопе, политика фото у новостей, 2-й eSIM (Saily).
- **Живые проверки перед промо** (не автоматизируются): сквозной `/go/`-редирект до лендинга партнёра + долёт SubID;
  реальный тач лайтбокса/Leaflet; свежесть кэша PWA SW; реально ли отрабатывает авто-ребилд новостей.
- **Контент-рост (по желанию):** уникальные тела ресторанных карточек; `{год}` в title денежных статей;
  наполнение «Развлечений»/«Услуг» реальными данными (тогда — спонсор-слоты); казино (через `research-collector`).
- **Будущая крупная фича:** авто-агрегатор новостей «в 1 клик» (XL, не начато; ТЗ — `ROADMAP.md` + память `news-aggregator-idea`).

---

## 2. Монетизация (состояние)

- **Партнёры** (`src/data/partners.json`, 7 шт., все через `/go/{partner}?c={slug}`, `rel="sponsored nofollow noopener"`,
  новая вкладка): Trip.com — `trip-carhire`/`trip-flights`/`trip-hotels`/`trip-transfers`/`trip-tours`
  (Allianceid 8701645, SID 319548076, trip_sub3 D17960553); **`safetywing`** (referenceID 26545532); **`airalo`** (eSIM).
- **Инвентарь:** AffiliateBox (1–3 на материал), золотая авто-витрина партнёров на главной (`ShowcaseRail`,
  единственная санкционированная авто-карусель — §10), бэнд «Услуги для жизни», спонсор-карточки в директориях
  (золотая рамка = маркёр платного). Прямая реклама — этап 2 (§16).
- **EKTA — НЕ подключаем** (партнёра нет; решение владельца 2026-06-17). В uk-контенте упоминается информационно, без `/go/`.
- **Ждёт владельца:** 2-й eSIM Saily (резерв), реальные сервисы в «Услугах», локальный прокат/экскурсии помимо Trip.com.

---

## 3. Стандарты формата (действуют — см. `CLAUDE.md` «Обновления формата»)

- **Фото обязательны у всего контента.** Ключевые — raw `<figure class="figure">` в .md (alt + figcaption «что на фото» +
  CC-кредит дословно, `width`/`height` → 0 CLS, `loading="lazy"`); остальные — `gallery`. Каждое фото один раз.
  Источники: Wikimedia/Openverse/Unsplash (ключ в `.env`, gitignored). Атрибуцию проверять, не из имени файла.
- **Факты не выдумывать (правило 4):** цены/адреса/часы — только из брифа/надёжного источника + «уточняйте»; иначе TODO.
- **Языковая пара:** одинаковый slug, совпадение числовых полей, те же `gallery` src; половину пары не публиковать.
- **Партнёрские ссылки — только `/go/`.** **FAQ убран со всего сайта** (нет FAQBlock/FAQPage). Порядок в хабах — `featuredOrder`.
- **После каждой доработки** синхронизировать `PROGRESS`/`HANDOFF` (+ `SPEC`/`CLAUDE`/`ROADMAP`/`AUDIT*` при изменении правил)
  + авто-память и её зеркало `docs/memory/`. Спонсор `services` = только золотая рамка (без текст-бейджа, решение владельца).

---

## 4. Нужно от владельца (блокеры/решения)

- **Отключить Cloudflare Web Analytics** в дашборде (см. §1) — единственное «техническое» действие на его стороне.
- **Решения §7 аудита** (см. §1): маркёры платного / `cities` / ширина колонки / фото новостей / Saily.
- **Факты для контента:** реальные данные заведений «Еды»/«Услуг»/казино; регуляторика релокации (ВНЖ/ИП/гражданство) —
  только с офиц. источниками. Цены/часы не выдумывать.
- ⚠️ GSC: TXT-запись `google-site-verification=…` в Cloudflare НЕ удалять. После locale-свопа следить за индексацией.

---

## 5. Быстрый старт для нового чата

1. Прочитать: `HANDOFF.md` (этот) → `README.md` → `CLAUDE.md` → `SPEC.md` → `PROGRESS.md` → `docs/memory/MEMORY.md`.
2. Новый материал — через **skill `add-content`** или вручную: `npm run new -- <тип> <slug> --title "…"` →
   заполнить ru → фото (гибрид: `photo-researcher` тянет CC, отбор «глазами» в осн. чате) → переводы uk/en
   (`uk-translator`/`en-translator`) → `fact-checker` → `content-editor` → гейты → снять `draft` → коммит пары/тройки.
3. Полный аудит сайта — **skill `full-audit`** (мультиагентный Workflow: аудит → верификация → синтез).
4. Перед каждым коммитом: **`npm run qa` → GO**; затем коммит+пуш в `main` (стандартная авторизация,
   память `always-commit-push-main`) и синхрон доков/памяти.
